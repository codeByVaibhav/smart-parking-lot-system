# Database URL for Prisma (SQLite by default for local dev)
DATABASE_URL="file:./dev.db"

# Server
PORT=3000

# Fee rates (per hour) in cents
FEE_MOTORCYCLE_PER_HOUR=100
FEE_CAR_PER_HOUR=200
FEE_BUS_PER_HOUR=500

# Base fee in cents (applied once)
BASE_FEE_MOTORCYCLE=50
BASE_FEE_CAR=100
BASE_FEE_BUS=200
